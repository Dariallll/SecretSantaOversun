# 🚀 Быстрое решение: доступ с других устройств

## Проблема
Сайт работает только на вашем компьютере (`localhost:5000`), но не доступен с других устройств.

## Быстрое решение (3 шага):

### 1️⃣ Узнайте ваш IP-адрес
Запустите в PowerShell:
```powershell
ipconfig | findstr IPv4
```
Или запустите скрипт:
```bash
python check_network.py
```

**Ваш IP скорее всего:** `192.168.1.57` (из вывода выше)

### 2️⃣ Откройте порт в файрволе Windows

**Через PowerShell (от администратора):**
```powershell
New-NetFirewallRule -DisplayName "Flask App" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow
```

**Или вручную:**
1. Откройте "Брандмауэр Защитника Windows"
2. "Дополнительные параметры" → "Правила для входящих подключений"
3. "Создать правило" → "Порт" → TCP → 5000
4. "Разрешить подключение"

### 3️⃣ Используйте IP вместо localhost

На других устройствах в той же Wi-Fi сети откройте:
```
http://192.168.1.57:5000
```

**Готово!** 🎉

---

## ⚠️ Если не работает:

### Вариант A: Используйте ngrok (работает везде)
```bash
# Установите ngrok с https://ngrok.com
ngrok http 5000
# Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
```

### Вариант B: Разверните на хостинге (постоянное решение)
См. `DEPLOY_SIMPLE.md` - разверните на Render.com за 5 минут!

---

## 📋 Чеклист:

- [ ] Приложение запущено (`python app.py`)
- [ ] Узнали IP-адрес компьютера
- [ ] Открыли порт 5000 в файрволе
- [ ] Устройства в одной Wi-Fi сети
- [ ] Используете `http://IP:5000` вместо `localhost:5000`

