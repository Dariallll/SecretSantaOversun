# Тайный Санта OVERSUN 🎅

Корпоративный веб-сервис для проведения новогодней игры "Тайный Санта" в компании OVERSUN.

## Описание

Внутренний веб-сайт для автоматизации проведения игры "Тайный Санта". Позволяет сотрудникам регистрироваться, указывать пожелания к подарку, и после жеребьёвки узнавать, кому они дарят подарок. Администратор имеет полный контроль над процессом.

## Функциональность

### Для участников:
- ✅ Регистрация в системе
- ✅ Указание пожеланий к подарку
- ✅ Просмотр информации о получателе подарка (после жеребьёвки)
- ✅ Новогоднее поздравление и напоминания

### Для администратора:
- ✅ Просмотр списка всех участников
- ✅ Управление этапами игры
- ✅ Запуск жеребьёвки
- ✅ Просмотр всех пар распределения
- ✅ Сброс игры

## Технологии

- **Backend**: Flask (Python)
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **База данных**: SQLite
- **Архитектура**: MVC

## Установка и запуск

### Требования
- Python 3.7 или выше
- pip (менеджер пакетов Python)

### Шаги установки

1. **Клонируйте или скачайте проект**

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте конфигурацию (опционально):**
   
   Откройте файл `config.py` и при необходимости измените:
   - `ADMIN_CODE` - код доступа администратора (по умолчанию: "oversun2025")
   - `SECRET_KEY` - секретный ключ Flask (для продакшена измените на случайную строку)

4. **Запустите приложение:**
   ```bash
   python app.py
   ```

5. **Откройте в браузере:**
   ```
   http://localhost:5000
   ```

### Доступ с других устройств

Если нужно, чтобы сайт был доступен с других устройств (телефонов, планшетов):

1. **Узнайте IP-адрес вашего компьютера:**
   ```bash
   python check_network.py
   # или
   ipconfig | findstr IPv4  # Windows
   ```

2. **Откройте порт в файрволе:**
   ```powershell
   New-NetFirewallRule -DisplayName "Flask App" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow
   ```

3. **На других устройствах в той же Wi-Fi сети откройте:**
   ```
   http://ВАШ_IP:5000
   ```

**Подробная инструкция:** см. `NETWORK_ACCESS.md` или `QUICK_NETWORK_FIX.md`

## Использование

### Для участников:

1. Перейдите на главную страницу
2. Нажмите "Принять участие"
3. Заполните форму регистрации:
   - Имя и фамилия (обязательно)
   - Email / внутренний ID (обязательно)
   - Пожелания к подарку (необязательно)
4. После регистрации вы попадете в личный кабинет
5. После жеребьёвки (когда администратор её проведет) вы увидите информацию о получателе подарка

### Для администратора:

1. На главной странице нажмите "Вход администратора"
2. Введите код администратора (по умолчанию: `oversun2025`)
3. В административной панели вы можете:
   - Просматривать список участников
   - Запустить жеребьёвку (когда все зарегистрировались)
   - Просматривать все пары распределения
   - Сбросить игру (если необходимо начать заново)

## Структура проекта

```
тайный санта/
├── app.py                 # Главный файл Flask приложения
├── database.py            # Модели и работа с БД
├── config.py              # Конфигурация
├── static/
│   ├── css/
│   │   └── style.css      # Стили приложения
│   └── js/
│       └── main.js        # Клиентский JavaScript
├── templates/
│   ├── index.html         # Главная страница
│   ├── register.html      # Регистрация участника
│   ├── participant.html   # Личный кабинет участника
│   ├── admin.html         # Административная панель
│   └── error.html         # Страница ошибки
├── requirements.txt       # Зависимости Python
├── README.md              # Этот файл
└── ARCHITECTURE.md        # Описание архитектуры
```

## База данных

База данных SQLite создается автоматически при первом запуске приложения. Файл базы данных: `secret_santa.db`

### Таблицы:

- **participants** - участники игры
- **game_state** - состояние игры

## Безопасность

⚠️ **Важно для продакшена:**

1. Измените `ADMIN_CODE` в `config.py` на сложный код
2. Измените `SECRET_KEY` в `app.py` на случайную строку
3. Не используйте `debug=True` в продакшене
4. Используйте HTTPS для защиты данных
5. Ограничьте доступ к серверу только внутренней сетью компании

## Алгоритм распределения

Система использует алгоритм циклической перестановки с проверкой самоназначения:
1. Создается список всех участников
2. Выполняется случайная перестановка
3. Проверяется отсутствие самоназначения
4. При необходимости выполняется циклический сдвиг
5. Пары сохраняются в базу данных

## Этапы игры

1. **Регистрация** - участники регистрируются в системе
2. **Жеребьёвка** - администратор запускает распределение
3. **Завершение** - участники видят своих получателей

## Поддержка

Для вопросов и предложений обращайтесь к разработчику или администратору системы.

## Лицензия

Внутренний проект компании OVERSUN. Использование только внутри компании.

---

**Разработано для OVERSUN** 🎄🎅

