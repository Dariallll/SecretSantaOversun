"""
Скрипт для проверки сетевого доступа к приложению
"""

import socket
import subprocess
import sys

def get_local_ip():
    """Получить локальный IP-адрес"""
    try:
        # Подключаемся к внешнему адресу, чтобы узнать локальный IP
        s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        s.connect(("8.8.8.8", 80))
        ip = s.getsockname()[0]
        s.close()
        return ip
    except Exception:
        return "Не удалось определить"

def check_port_open(port=5000):
    """Проверить, открыт ли порт"""
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.settimeout(1)
    result = sock.connect_ex(('127.0.0.1', port))
    sock.close()
    return result == 0

def main():
    print("=" * 50)
    print("Проверка сетевого доступа к приложению")
    print("=" * 50)
    
    # Получаем IP
    local_ip = get_local_ip()
    print(f"\n📍 Ваш локальный IP-адрес: {local_ip}")
    
    # Проверяем порт
    if check_port_open(5000):
        print("✅ Порт 5000 открыт и доступен")
    else:
        print("❌ Порт 5000 не отвечает")
        print("   Убедитесь, что приложение запущено: python app.py")
        return
    
    print("\n" + "=" * 50)
    print("📱 Для доступа с других устройств:")
    print("=" * 50)
    print(f"\n1. Убедитесь, что устройства в одной Wi-Fi сети")
    print(f"2. Откройте в браузере на другом устройстве:")
    print(f"\n   http://{local_ip}:5000")
    print(f"\n3. Если не работает, проверьте файрвол Windows")
    print("\n" + "=" * 50)
    print("💡 Альтернатива: используйте ngrok для доступа из интернета")
    print("   ngrok http 5000")
    print("=" * 50)

if __name__ == '__main__':
    main()

