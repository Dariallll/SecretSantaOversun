# Развертывание приложения "Тайный Санта OVERSUN"

## Вариант 1: Render.com (Рекомендуется - бесплатно)

### Шаги развертывания:

1. **Создайте аккаунт на Render.com**
   - Перейдите на https://render.com
   - Зарегистрируйтесь (можно через GitHub)

2. **Подготовьте репозиторий GitHub**
   - Создайте репозиторий на GitHub
   - Загрузите туда все файлы проекта
   - Убедитесь, что файлы `requirements.txt`, `Procfile` и `render.yaml` включены

3. **Развертывание на Render:**
   - Войдите в Render Dashboard
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Render автоматически определит настройки из `render.yaml`
   - Или заполните вручную:
     - **Name**: secret-santa-oversun
     - **Environment**: Python 3
     - **Build Command**: `pip install -r requirements.txt`
     - **Start Command**: `gunicorn app:app`
   - Нажмите "Create Web Service"

4. **Настройка переменных окружения:**
   - В настройках сервиса перейдите в "Environment"
   - Добавьте переменную:
     - `ADMIN_CODE` = ваш код администратора (например, `oversun2025`)
   - Опционально:
     - `SECRET_KEY` = случайная строка для безопасности

5. **Получите ссылку:**
   - После деплоя Render предоставит URL вида: `https://secret-santa-oversun.onrender.com`
   - Эта ссылка будет доступна из интернета!

### Важно для Render:
- Первый запуск может занять 2-3 минуты
- Бесплатный тариф может "засыпать" после 15 минут бездействия
- Для постоянной работы рассмотрите платный тариф

---

## Вариант 2: Railway.app (Альтернатива)

1. **Создайте аккаунт на Railway.app**
   - https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - "New Project" → "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте переменные:**
   - В настройках проекта добавьте:
     - `ADMIN_CODE` = ваш код
     - `PORT` = Railway установит автоматически

4. **Railway автоматически определит Python проект и развернет**

---

## Вариант 3: PythonAnywhere (Простой вариант)

1. **Создайте бесплатный аккаунт:**
   - https://www.pythonanywhere.com

2. **Загрузите файлы:**
   - Через веб-интерфейс загрузите все файлы проекта
   - Или используйте Git

3. **Настройте веб-приложение:**
   - В разделе "Web" создайте новое приложение
   - Укажите путь к `app.py`
   - Настройте WSGI файл

4. **Получите ссылку:**
   - Будет доступна по адресу: `ваш-username.pythonanywhere.com`

---

## Вариант 4: Быстрый тест через ngrok (Временный доступ)

Если нужно быстро протестировать локально с доступом из интернета:

1. **Установите ngrok:**
   ```bash
   # Скачайте с https://ngrok.com
   # Или через pip: pip install pyngrok
   ```

2. **Запустите приложение локально:**
   ```bash
   python app.py
   ```

3. **В другом терминале запустите ngrok:**
   ```bash
   ngrok http 5000
   ```

4. **Используйте предоставленный ngrok URL** (например: `https://abc123.ngrok.io`)
   - ⚠️ URL меняется при каждом перезапуске ngrok
   - ⚠️ Это только для тестирования!

---

## Рекомендации для продакшена:

1. **Измените код администратора:**
   - В настройках хостинга установите переменную окружения `ADMIN_CODE`
   - Или измените в `config.py` перед деплоем

2. **Измените SECRET_KEY:**
   - Используйте случайную строку
   - Можно сгенерировать: `python -c "import secrets; print(secrets.token_hex(32))"`

3. **Настройте домен (опционально):**
   - На Render/Railway можно подключить свой домен

4. **Отключите debug режим:**
   - В `app.py` измените `app.run(debug=True)` на `app.run(debug=False)`

---

## Быстрый старт с Render (самый простой способ):

1. Загрузите проект на GitHub
2. Зайдите на render.com
3. "New +" → "Web Service"
4. Подключите репозиторий
5. Render автоматически развернет приложение
6. Получите публичную ссылку!

**Готово! Теперь у вас есть рабочая ссылка для обмена! 🎉**

