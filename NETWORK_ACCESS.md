# 🌐 Доступ к сайту с других устройств

## Проблема
Приложение запущено локально и доступно только на `localhost:5000`. Для доступа с других устройств нужно настроить сетевой доступ.

## Решение 1: Доступ в локальной сети (Wi-Fi) 📱

### Шаг 1: Узнайте IP-адрес вашего компьютера

**Windows:**
```powershell
ipconfig
```
Найдите "IPv4-адрес" (например: `192.168.1.100`)

**Mac/Linux:**
```bash
ifconfig
# или
ip addr
```

### Шаг 2: Убедитесь, что приложение запущено с правильным хостом

Приложение уже настроено на `host='0.0.0.0'`, что правильно. Просто запустите:
```bash
python app.py
```

### Шаг 3: Откройте порт в файрволе Windows

1. Откройте "Брандмауэр Защитника Windows"
2. Нажмите "Дополнительные параметры"
3. Выберите "Правила для входящих подключений" → "Создать правило"
4. Выберите "Порт" → "TCP" → "Определенные локальные порты: 5000"
5. Разрешите подключение
6. Примените ко всем профилям
7. Назовите правило "Flask App"

**Или через PowerShell (от администратора):**
```powershell
New-NetFirewallRule -DisplayName "Flask App" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow
```

### Шаг 4: Используйте IP-адрес вместо localhost

На других устройствах в той же Wi-Fi сети откройте:
```
http://ВАШ_IP:5000
```
Например: `http://192.168.1.100:5000`

---

## Решение 2: Быстрый доступ через ngrok (для тестирования) 🚀

### Установка ngrok:

1. Скачайте с https://ngrok.com/download
2. Распакуйте в папку проекта
3. Или установите через pip:
```bash
pip install pyngrok
```

### Использование:

1. **Запустите приложение:**
```bash
python app.py
```

2. **В другом терминале запустите ngrok:**
```bash
ngrok http 5000
```

3. **Скопируйте HTTPS URL** (например: `https://abc123.ngrok.io`)
   - Этот URL будет работать из любой точки мира!
   - ⚠️ URL меняется при каждом перезапуске ngrok
   - ⚠️ Это только для тестирования

---

## Решение 3: Развертывание на хостинге (постоянное решение) ☁️

### Render.com (рекомендуется - бесплатно):

1. Загрузите проект на GitHub
2. Перейдите на https://render.com
3. "New +" → "Web Service"
4. Подключите репозиторий
5. Получите постоянную ссылку вида: `https://secret-santa-oversun.onrender.com`

**Подробная инструкция:** см. `DEPLOY_SIMPLE.md`

---

## Решение 4: Использование мобильной точки доступа 📶

Если устройства не в одной сети:

1. Создайте точку доступа на вашем компьютере
2. Подключите другие устройства к этой точке доступа
3. Используйте IP-адрес компьютера (как в Решении 1)

---

## Проверка доступности

### На компьютере с приложением:
```bash
# Проверьте, что приложение слушает на всех интерфейсах
netstat -an | findstr :5000
```

Должно быть: `0.0.0.0:5000` или `:::5000`

### С другого устройства:
```bash
# Проверьте доступность
ping ВАШ_IP
curl http://ВАШ_IP:5000
```

---

## Частые проблемы

### ❌ "Сайт недоступен"
- Проверьте, что приложение запущено
- Проверьте IP-адрес
- Проверьте файрвол

### ❌ "Подключение отклонено"
- Откройте порт 5000 в файрволе
- Убедитесь, что устройства в одной сети

### ❌ "Таймаут"
- Проверьте, что используете правильный IP
- Убедитесь, что роутер не блокирует локальные подключения

---

## Рекомендация

Для постоянного использования лучше всего развернуть на **Render.com** или **Railway.app** - это даст вам постоянную ссылку, которой можно делиться.

